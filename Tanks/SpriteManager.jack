class SpriteManager{
    static Sprite bigA, bigB, bigD, bigE, bigF, bigG, bigI, bigK, bigM;      
    static Sprite bigN, bigO, bigP, bigR, bigS, bigT, bigU, bigW, bigY, bigV;  
    static Sprite selectTriangle, playerTankUp, playerTankRight;

    function void loadBigF() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigF = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigF() {
        do bigF.dispose();
        return;
    }

    function Sprite getBigF() {
        return bigF;
    }

    function void loadBigI() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
         let line[0] = 15360;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigI = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigI() {
        do bigI.dispose();
        return;
    }

    function Sprite getBigI() {
        return bigI;
    }

    function void loadBigR() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 15;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 31;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 63;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 126;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 126;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 63;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 31;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 15;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 32767;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 127;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 255;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 511;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 1023;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 2047;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 4079;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 8143;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 16271;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 32527;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = -497;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = -1009;
        let line[1] = 1;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = -2033;
        let line[1] = 3;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = -4081;
        let line[1] = 7;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 15;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 31;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 63;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 127;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 252;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigR = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigR() {
        do bigR.dispose();
        return;
    }

    function Sprite getBigR() {
        return bigR;
    }
    
    function void loadBigP() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 15;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 31;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 63;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 126;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 126;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 63;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 31;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 15;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 32767;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigP = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigP() {
        do bigP.dispose();
        return;
    }

    function Sprite getBigP() {
        return bigP;
    }
    
    function void loadBigK() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 252;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 127;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = -32763;
        let line[1] = 63;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 31;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 15;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = -4081;
        let line[1] = 7;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = -2033;
        let line[1] = 3;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = -1009;
        let line[1] = 1;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = -497;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 32527;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 16271;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 8143;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 4095;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 2047;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 1023;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 511;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 255;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 127;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 127;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 255;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 511;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 1023;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 2047;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 4079;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 8143;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 16271;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 32527;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = -497;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = -1009;
        let line[1] = 1;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = -2033;
        let line[1] = 3;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = -4081;
        let line[1] = 7;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 15;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 31;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 63;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 127;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 252;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigK = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigK() {
        do bigK.dispose();
        return;
    }

    function Sprite getBigK() {
        return bigK;
    }

    function void loadBigE() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigE = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigE() {
        do bigE.dispose();
        return;
    }

    function Sprite getBigE() {
        return bigE;
    }

    function void loadBigG() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -128;
        let line[1] = 7;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -32;
        let line[1] = 7;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 31;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 248;
        let line[1] = 30;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 124;
        let line[1] = 60;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 62;
        let line[1] = 60;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 56;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 255;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 255;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 255;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 255;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 60;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 30;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 62;
        let line[1] = 30;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 124;
        let line[1] = 30;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 124;
        let line[1] = 31;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -32264;
        let line[1] = 15;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -15376;
        let line[1] = 15;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 7;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -32;
        let line[1] = 7;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -64;
        let line[1] = 3;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = -128;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigG = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigG() {
        do bigG.dispose();
        return;
    }

    function Sprite getBigG() {
        return bigG;
    }

    function void loadBigA() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 32256;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 32256;
        let line[1] = 0;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 0;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 0;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 0;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = -6272;
        let line[1] = 0;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = -6272;
        let line[1] = 1;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = -6272;
        let line[1] = 1;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = -15424;
        let line[1] = 1;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 3;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 7;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 15;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 31;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 31;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 31;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 31;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 248;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 248;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 248;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 248;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigA = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigA() {
        do bigA.dispose();
        return;
    }

    function Sprite getBigA() {
        return bigA;
    }

    function void loadBigM() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -32576;
        let line[1] = 1;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -32544;
        let line[1] = 1;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -15888;
        let line[1] = 3;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -15888;
        let line[1] = 7;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -15880;
        let line[1] = 7;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = -15944;
        let line[1] = 15;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = -15944;
        let line[1] = 14;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = -15940;
        let line[1] = 14;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = -15972;
        let line[1] = 30;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = -15972;
        let line[1] = 28;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = -15970;
        let line[1] = 28;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = -15970;
        let line[1] = 60;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = -15986;
        let line[1] = 56;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = -15986;
        let line[1] = 56;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = -15986;
        let line[1] = 56;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = -15986;
        let line[1] = 56;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 25486;
        let line[1] = 56;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 25358;
        let line[1] = 56;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 30478;
        let line[1] = 56;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 13838;
        let line[1] = 56;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 13838;
        let line[1] = 56;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 13831;
        let line[1] = 112;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15879;
        let line[1] = 112;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15879;
        let line[1] = 112;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 7175;
        let line[1] = 112;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 7175;
        let line[1] = 112;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 7175;
        let line[1] = 112;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 7175;
        let line[1] = 112;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 2055;
        let line[1] = 112;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigM = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigM() {
        do bigM.dispose();
        return;
    }

    function Sprite getBigM() {
        return bigM;
    }

    function void loadBigS() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -64;
        let line[1] = 1;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 7;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 15;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 124;
        let line[1] = 31;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 62;
        let line[1] = 62;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 124;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 112;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 192;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 192;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 126;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 248;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 496;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 992;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 3968;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 7936;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15872;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -2048;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -4096;
        let line[1] = 3;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -16384;
        let line[1] = 7;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 31;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 62;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 124;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 120;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 240;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 224;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 224;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 224;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 224;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 3;
        let line[1] = 224;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 240;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 248;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 120;
        let line[1] = 124;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 248;
        let line[1] = 62;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 1008;
        let line[1] = 31;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -32;
        let line[1] = 15;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -128;
        let line[1] = 7;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = -512;
        let line[1] = 1;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigS = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigS() {
        do bigS.dispose();
        return;
    }

    function Sprite getBigS() {
        return bigS;
    }

    function void loadBigT() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 255;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigT = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigT() {
        do bigT.dispose();
        return;
    }

    function Sprite getBigT() {
        return bigT;
    }

    function void loadBigU() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[10] = line;

        let line = Array.new(2);
         let line[0] = 15;
        let line[1] = 240;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 124;
        let line[1] = 62;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 248;
        let line[1] = 31;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -32272;
        let line[1] = 15;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -6160;
        let line[1] = 15;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -32;
        let line[1] = 7;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -64;
        let line[1] = 3;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigU = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigU() {
        do bigU.dispose();
        return;
    }

    function Sprite getBigU() {
        return bigU;
    }

    function void loadBigD() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 1;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 1;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 3;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 3;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 7;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 15;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 15;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 30;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 30;
        let data[10] = line;

        let line = Array.new(2);
         let line[0] = 15;
        let line[1] = 60;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 30;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 30;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 15;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 15;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 7;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 3;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -2033;
        let line[1] = 1;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 32767;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 8191;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 2047;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigD = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigD() {
        do bigD.dispose();
        return;
    }

    function Sprite getBigD() {
        return bigD;
    }

    function void loadBigO() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 31744;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -512;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 1;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -14464;
        let line[1] = 3;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[10] = line;

        let line = Array.new(2);
         let line[0] = 56;
        let line[1] = 28;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 24;
        let line[1] = 24;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 6;
        let line[1] = 96;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 60;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 15;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 7;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -15424;
        let line[1] = 3;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -6272;
        let line[1] = 1;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 1;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -512;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 31744;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigO = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigO() {
        do bigO.dispose();
        return;
    }

    function Sprite getBigO() {
        return bigO;
    }

    function void loadBigN() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 31;
        let line[1] = 240;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 240;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 63;
        let line[1] = 240;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 127;
        let line[1] = 240;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 127;
        let line[1] = 240;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 255;
        let line[1] = 240;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 255;
        let line[1] = 240;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 511;
        let line[1] = 240;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 495;
        let line[1] = 240;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 495;
        let line[1] = 240;
        let data[10] = line;

        let line = Array.new(2);
         let line[0] = 975;
        let line[1] = 240;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 975;
        let line[1] = 240;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 1999;
        let line[1] = 240;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 1935;
        let line[1] = 240;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 3983;
        let line[1] = 240;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 3855;
        let line[1] = 240;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 3855;
        let line[1] = 240;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 7951;
        let line[1] = 240;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 7695;
        let line[1] = 240;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 7695;
        let line[1] = 240;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 15375;
        let line[1] = 240;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 15375;
        let line[1] = 240;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 31759;
        let line[1] = 240;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 30735;
        let line[1] = 240;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = -4081;
        let line[1] = 240;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = -4081;
        let line[1] = 240;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 241;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 241;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 243;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 243;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 247;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 247;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 247;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 255;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 255;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 254;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 252;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 252;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 248;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigN = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigN() {
        do bigN.dispose();
        return;
    }

    function Sprite getBigN() {
        return bigN;
    }

    function void loadBigB() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 7;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 31;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 62;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 124;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 60;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 62;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 31;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -16369;
        let line[1] = 31;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 7;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 1;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 1;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 3;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 7;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 15;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 30;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 28;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 56;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 224;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 120;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 126;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -32753;
        let line[1] = 63;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -8177;
        let line[1] = 31;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 7;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = -1;
        let line[1] = 1;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 16383;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigB = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigB() {
        do bigB.dispose();
        return;
    }

    function Sprite getBigB() {
        return bigB;
    }

    function void loadBigY() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 240;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 15;
        let line[1] = 112;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 120;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 30;
        let line[1] = 56;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 60;
        let line[1] = 60;
        let data[10] = line;

        let line = Array.new(2);
         let line[0] = 60;
        let line[1] = 28;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 120;
        let line[1] = 30;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 15;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = -32288;
        let line[1] = 7;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = -32288;
        let line[1] = 7;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = -6208;
        let line[1] = 3;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = -64;
        let line[1] = 1;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = -128;
        let line[1] = 1;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 0;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 32256;
        let line[1] = 0;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigY = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigY() {
        do bigY.dispose();
        return;
    }

    function Sprite getBigY() {
        return bigY;
    }

     function void loadBigV() {
        var Array data, line;

        let data = Array.new(48);

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 7;
        let line[1] = 224;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 112;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 14;
        let line[1] = 56;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 28;
        let line[1] = 56;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = 56;
        let line[1] = 28;
        let data[15] = line;
        
        let line = Array.new(2);
        let line[0] = 120;
        let line[1] = 30;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 120;
        let line[1] = 30;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 112;
        let line[1] = 14;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 15;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 15;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 240;
        let line[1] = 15;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 224;
        let line[1] = 7;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = -32288;
        let line[1] = 7;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = -32288;
        let line[1] = 7;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = -32288;
        let line[1] = 7;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = -32320;
        let line[1] = 3;
        let data[31] = line;

        let line = Array.new(2);
        let line[0] = -15424;
        let line[1] = 3;
        let data[32] = line;

        let line = Array.new(2);
        let line[0] = -15424;
        let line[1] = 3;
        let data[33] = line;

        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[34] = line;

        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[35] = line;

        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[36] = line;
        
        let line = Array.new(2);
        let line[0] = -15488;
        let line[1] = 1;
        let data[37] = line;

        let line = Array.new(2);
        let line[0] = -6272;
        let line[1] = 1;
        let data[38] = line;

        let line = Array.new(2);
        let line[0] = -6400;
        let line[1] = 0;
        let data[39] = line;

        let line = Array.new(2);
        let line[0] = -6400;
        let line[1] = 0;
        let data[40] = line;

        let line = Array.new(2);
        let line[0] = -6400;
        let line[1] = 0;
        let data[41] = line;

        let line = Array.new(2);
        let line[0] = -6400;
        let line[1] = 0;
        let data[42] = line;

        let line = Array.new(2);
        let line[0] = 32256;
        let line[1] = 0;
        let data[43] = line;

        let line = Array.new(2);
        let line[0] = 15360;
        let line[1] = 0;
        let data[44] = line;

        let line = Array.new(2);
        let line[0] = 6144;
        let line[1] = 0;
        let data[45] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[46] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[47] = line;

        let bigV = Sprite.new(32, 48, data);
        return;
    }

    function void disposeBigV() {
        do bigV.dispose();
        return;
    }

    function Sprite getBigV() {
        return bigV;
    }

    function void loadSelectTriangle() {
        var Array data, line; 
        
        let data = Array.new(16);

        let line = Array.new(1);
        let line[0] = 0;
        let data[0] = line;

        let line = Array.new(1);
        let line[0] = 0;
        let data[1] = line;

        let line = Array.new(1);
        let line[0] = 7;
        let data[2] = line;

        let line = Array.new(1);
        let line[0] = 31;
        let data[3] = line;

        let line = Array.new(1);
        let line[0] = 127;
        let data[4] = line;

        let line = Array.new(1);
        let line[0] = 511;
        let data[5] = line;

        let line = Array.new(1);
        let line[0] = 2047;
        let data[6] = line;

        let line = Array.new(1);
        let line[0] = 4095;
        let data[7] = line;

        let line = Array.new(1);
        let line[0] = 4095;
        let data[8] = line;

        let line = Array.new(1);
        let line[0] = 2047;
        let data[9] = line;

        let line = Array.new(1);
        let line[0] = 511;
        let data[10] = line;

        let line = Array.new(1);
        let line[0] = 127;
        let data[11] = line;

        let line = Array.new(1);
        let line[0] = 31;
        let data[12] = line;

        let line = Array.new(1);
        let line[0] = 7;
        let data[13] = line;

        let line = Array.new(1);
        let line[0] = 0;
        let data[14] = line;

        let line = Array.new(1);
        let line[0] = 0;
        let data[15] = line;    

        let selectTriangle = Sprite.new(16, 16, data);
        return;
    }

    function void disposeSelectTriangle() {
        do selectTriangle.dispose();
        return;
    }

    function Sprite getSelectTriangle() {
        return selectTriangle;
    }

    function void loadPlayerTankUp() {
        var Array data, line; 
        
        let data = Array.new(32);

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = ~32767;
        let line[1] = 1;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 255;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = -4;
        let line[1] = 16383;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = -32380;
        let line[1] = 8577;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = -32260;
        let line[1] = 16257;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = -32380;
        let line[1] = 8577;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = -7684;
        let line[1] = 16263;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = -3708;
        let line[1] = 8591;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = -17924;
        let line[1] = 16285;
        let data[15] = line;  

        let line = Array.new(2);
        let line[0] = -25212;
        let line[1] = 8633;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 3580;
        let line[1] = 16304;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 3460;
        let line[1] = 8624;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = 3580;
        let line[1] = 16304;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = 6532;
        let line[1] = 8632;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = 31228;
        let line[1] = 16286;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = -3708;
        let line[1] = 8591;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -15876;
        let line[1] = 16259;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = 388;
        let line[1] = 8576;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 508;
        let line[1] = 16256;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 388;
        let line[1] = 8576;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 508;
        let line[1] = 16256;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = -128;
        let line[1] = 255;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = -256;
        let line[1] = 127;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[31] = line;

        let playerTankUp = Sprite.new(32, 32, data);
        return;
    }

    function void disposePlayerTankUp() {
        do playerTankUp.dispose();
        return;
    }

    function Sprite getPlayerTankUp() {
        return playerTankUp;
    }

    function void loadPlayerTankRight() {
        var Array data, line; 
        
        let data = Array.new(32);

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[0] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[1] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 127;
        let data[2] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[3] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[4] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[5] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[6] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 127;
        let data[7] = line;

        let line = Array.new(2);
        let line[0] = -4;
        let line[1] = 255;
        let data[8] = line;

        let line = Array.new(2);
        let line[0] = 12;
        let line[1] = 192;
        let data[9] = line;

        let line = Array.new(2);
        let line[0] = -4084;
        let line[1] = 192;
        let data[10] = line;

        let line = Array.new(2);
        let line[0] = -1012;
        let line[1] = 193;
        let data[11] = line;

        let line = Array.new(2);
        let line[0] = -29172;
        let line[1] = 195;
        let data[12] = line;

        let line = Array.new(2);
        let line[0] = 1548;
        let line[1] = 199;
        let data[13] = line;

        let line = Array.new(2);
        let line[0] = 1804;
        let line[1] = 198;
        let data[14] = line;

        let line = Array.new(2);
        let line[0] = -31988;
        let line[1] = 16383;
        let data[15] = line;  

        let line = Array.new(2);
        let line[0] = -31988;
        let line[1] = 16383;
        let data[16] = line;

        let line = Array.new(2);
        let line[0] = 1804;
        let line[1] = 198;
        let data[17] = line;

        let line = Array.new(2);
        let line[0] = 1548;
        let line[1] = 199;
        let data[18] = line;

        let line = Array.new(2);
        let line[0] = -29172;
        let line[1] = 195;
        let data[19] = line;

        let line = Array.new(2);
        let line[0] = -1012;
        let line[1] = 193;
        let data[20] = line;

        let line = Array.new(2);
        let line[0] = -2036;
        let line[1] = 192;
        let data[21] = line;

        let line = Array.new(2);
        let line[0] = 12;
        let line[1] = 192;
        let data[22] = line;

        let line = Array.new(2);
        let line[0] = -8;
        let line[1] = 255;
        let data[23] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 127;
        let data[24] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[25] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[26] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[27] = line;

        let line = Array.new(2);
        let line[0] = 21840;
        let line[1] = 85;
        let data[28] = line;

        let line = Array.new(2);
        let line[0] = -16;
        let line[1] = 127;
        let data[29] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[30] = line;

        let line = Array.new(2);
        let line[0] = 0;
        let line[1] = 0;
        let data[31] = line;

        let playerTankRight = Sprite.new(32, 32, data);
        return;
    }

    function void disposePlayerTankRight() {
        do playerTankRight.dispose();
        return;
    }

    function Sprite getPlayerTankRight() {
        return playerTankRight;
    }
}