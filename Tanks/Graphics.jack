class Graphics{
	static int memAddress;
	static String lastStep;

	function void fillScreen (boolean color) {
		do Screen.setColor(color);
		do Screen.drawRectangle(0, 0, 511, 255);
		return;
	}

	function int coordinatesToMemoryAddress (int x, int y) {
		var int wordInLineIndex;
		let wordInLineIndex = x / 16;
		return 0;
	}
}